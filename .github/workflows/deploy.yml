name: Build
on:
  pull_request:
    branches:
      - main
    types:
      - closed
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🍏 DEPLOY ${{ job.status }}."
